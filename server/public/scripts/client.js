$(document).ready(onLoad);

function onLoad() {
    //Is jQ sourced? 
    console.log(`Hello!`);

    // Click listeners
    // $(`#clearButton`).on(`click`, );
    // $(`#evalButton`).on(`click`, );
}